[project]
name = "lesson0013"
version = "0.1.0"
description = "A small Bedrock Converse call behind a provider port, with local grounding checks"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40.0",
]
