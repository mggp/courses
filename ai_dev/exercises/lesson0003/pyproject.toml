[project]
name = "lesson0003"
version = "0.1.0"
description = "Hybrid retrieval: BM25 + dense + RRF fusion"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "sentence-transformers>=5.7.0",
]
