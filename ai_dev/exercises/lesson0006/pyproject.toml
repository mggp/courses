[project]
name = "lesson0006"
version = "0.1.0"
description = "Reranking: a cross-encoder re-orders the fused candidate list"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "sentence-transformers>=5.7.0",
]
